apiVersion: v1
kind: Service
metadata:
  labels:
    app: pitstop
    pitstop.service: customermanagementapi
  name: customermanagementapi
spec:
  ports:
  - name: "5100"
    port: 5100
    targetPort: 5100
  selector:
    app: pitstop
    pitstop.service: customermanagementapi
status:
  loadBalancer: {}
